import {useContext,useState} from "react";import {NavLink,Link} from "react-router-dom";import {AppContext} from "../../context/AppContext";
const items=[{label:"Home",path:"/"},{label:"Courses",path:"/courses"},{label:"Roadmap",path:"/roadmap"},{label:"Dashboard",path:"/dashboard"},{label:"Achievements",path:"/achievements"},{label:"Profile",path:"/profile"}];
export default function Navbar(){const[open,setOpen]=useState(false);const{theme,toggleTheme,user,logout}=useContext(AppContext);const close=()=>setOpen(false);return <header className="site-header"><nav className="navbar container" aria-label="Main navigation"><Link className="brand" to="/" onClick={close} aria-label="SkillTrack home"><img className="brand-logo" src="/skilltrack-logo.png" alt="SkillTrack"/></Link><button className="menu-button" type="button" aria-expanded={open} onClick={()=>setOpen(v=>!v)} aria-label="Toggle navigation">☰</button><div className={open?"nav-links open":"nav-links"}>{items.map(i=><NavLink key={i.path} to={i.path} end={i.path==="/"} onClick={close}>{i.label}</NavLink>)}{user?.role==="admin"&&<NavLink to="/admin/courses" onClick={close}>Admin</NavLink>}{user?<button className="theme-button" onClick={()=>{logout();close()}}>Sign out</button>:<NavLink to="/login" onClick={close}>Sign in</NavLink>}<button className="theme-button" type="button" onClick={toggleTheme}>{theme==="light"?"Dark mode":"Light mode"}</button></div></nav></header>}
